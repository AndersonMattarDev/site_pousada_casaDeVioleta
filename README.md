# Casa de Violeta - Landing Page (skeleton)

Stack: Vite + React + TypeScript + TailwindCSS

How to run:

1. npm install
2. npm run dev

Replace placeholder images in src/components/Hero.tsx or update links in src/components/GallerySection.tsx
